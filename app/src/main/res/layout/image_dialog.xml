<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/relativeLayout"
    android:layout_width="match_parent"
    android:paddingBottom="16dp"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/dialog_camera"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/Widget.Material3.Button.IconButton"
        app:iconSize="28dp"
        android:layout_marginHorizontal="8dp"
        app:icon="@drawable/ic_camera"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/dialog_image"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/dialog_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/Widget.Material3.Button.IconButton"
        app:iconSize="28dp"
        android:layout_marginHorizontal="8dp"
        app:icon="@drawable/ic_image"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toEndOf="@+id/dialog_camera"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/camera_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/camera"
        style="@style/TextAppearance.Material3.TitleSmall"
        app:layout_constraintEnd_toEndOf="@+id/dialog_camera"
        app:layout_constraintStart_toStartOf="@+id/dialog_camera"
        app:layout_constraintTop_toBottomOf="@+id/dialog_camera" />

    <TextView
        android:id="@+id/image_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/TextAppearance.Material3.TitleSmall"
        android:text="@string/gallery"
        app:layout_constraintEnd_toEndOf="@+id/dialog_image"
        app:layout_constraintStart_toStartOf="@+id/dialog_image"
        app:layout_constraintTop_toBottomOf="@+id/dialog_image" />
</androidx.constraintlayout.widget.ConstraintLayout>